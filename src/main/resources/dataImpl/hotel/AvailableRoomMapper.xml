<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.hotel.data.hotel.AvailableRoomMapper">
    <insert id="insertAvailableRoom">
        insert into available_room(room_id, date, hotel_id, quantity)
        values (#{roomId},#{currentDate}, #{hotelId}, #{quantity})
    </insert>

</mapper>